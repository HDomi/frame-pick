/** 갤러리 섬네일 긴 변 상한 (OOM 방지) */
export const FRAME_THUMB_MAX_EDGE = 720

/** 캔버스 적용용 캡처 긴 변 상한 */
export const FRAME_APPLY_MAX_EDGE = 1920

/** 이 크기 초과 시 경고 토스트 (바이트) */
export const VIDEO_SIZE_WARN_BYTES = 200 * 1024 * 1024

/** duration Infinity 재시도 타임아웃 (ms) */
export const VIDEO_DURATION_RETRY_MS = 2500

/** seeked 미발생 시 폴백 타임아웃 (ms) */
export const VIDEO_SEEK_TIMEOUT_MS = 4000
